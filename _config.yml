highlighter: pygments
markdown: redcarpet
redcarpet:
  extensions:
    - no_intra_emphasis
    - autolink
    - strikethrough
    - fenced_code_blocks

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
